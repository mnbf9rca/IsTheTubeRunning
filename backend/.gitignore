
# Environment files (ignore all except specific ones)
.env*
.flaskenv*

# Allow encrypted environment files (dotenvx - values are encrypted)
!.env
!.env.ci
!.env.production
!.env.x

# Allow example file
!.env.example

# Ensure private keys are never committed
.env.keys
